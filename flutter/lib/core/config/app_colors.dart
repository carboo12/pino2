import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF2563EB);
  static const primaryDark = Color(0xFF1D4ED8);
  static const primaryLight = Color(0xFFDBEAFE);

  // Secondary
  static const secondary = Color(0xFF7C3AED);
  static const secondaryLight = Color(0xFFEDE9FE);

  // Accent
  static const accent = Color(0xFFF59E0B);
  static const accentLight = Color(0xFFFEF3C7);

  // Success
  static const success = Color(0xFF10B981);
  static const successLight = Color(0xFFD1FAE5);

  // Error
  static const error = Color(0xFFEF4444);
  static const errorLight = Color(0xFFFEE2E2);

  // Backgrounds
  static const background = Color(0xFFFAFBFC);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1E293B);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted = Color(0xFF94A3B8);

  // Border
  static const border = Color(0xFFE2E8F0);

  // Gradients
  static const heroGradient = [Color(0xFF2563EB), Color(0xFF7C3AED)];
  static const loginGradient = [Color(0xFFF8FAFC), Color(0xFFDBEAFE)];
}
