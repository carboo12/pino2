// La ruta histórica de Finanzas usa la misma pantalla canónica que Logística.
// Así se evita mantener dos contratos distintos para liquidaciones de ruta.
export { default } from '../routes/settlement-page';
